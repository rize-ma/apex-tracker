export type UserData = {
  availableSegments: Array<object>;
  metadata: object;
  platformInfo: object;
  segments: Array<object>;
  userInfo: object;
};

export type ErrorMessage = { data: { message: string } };
